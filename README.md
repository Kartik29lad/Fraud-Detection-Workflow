# Fraud-Detection-Workflow

TypeScript fraud detection API with MSSQL-backed scoring, review queues, and n8n notification workflow integration.

## Notes

- Keep environment variables in `src/.env`.
- Do not commit real API keys, database credentials, runtime logs, or `node_modules`.
- Run TypeScript checks with `npx tsc --noEmit`.
